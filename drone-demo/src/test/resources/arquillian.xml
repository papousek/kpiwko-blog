<?xml version="1.0" encoding="UTF-8"?>
<arquillian xmlns="http://jboss.org/schema/arquillian" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://jboss.org/schema/arquillian
        http://jboss.org/schema/arquillian/arquillian_1_0.xsd">

    <!-- Uncomment to have test archives exported to the file system for inspection -->
    <!-- <engine> -->
    <!-- <property name="deploymentExportPath">target/</property> -->
    <!-- </engine> -->

    <!-- Force the use of the Servlet 3.0 protocol with all containers, as it is the most mature -->
    <defaultProtocol type="Servlet 3.0" />

    <!-- Example configuration for a remote JBoss AS 7 instance -->
    <container qualifier="jboss" default="true">
        <!-- If you want to use the JBOSS_HOME environment variable, just delete the jbossHome property -->
        <configuration>
<!--             <property name="jbossHome">/opt/jboss-as-7.1.0.Final/</property> -->
        </configuration>
    </container>

    <container qualifier="openshift">
        <configuration>
            <property name="namespace">richfacesqa</property>
            <property name="application">ftest</property>
            <property name="sshUserName">dc5eed1b78e64b4db3be3c67b2d64cb7</property>
            <!-- Passphrase can be specified by defining the environment variable SSH_PASSPHRASE -->
            <property name="passphrase">${openshift.key.password}</property>
            <property name="login">richfaces.qa.jenkins@gmail.com</property>
            <property name="deploymentTimeoutInSeconds">120</property>
        </configuration>
    </container>

    <extension qualifier="ajocado">
        <property name="browser">*firefox</property>
    </extension>

    <extension qualifier="ajocado-firefox9">
        <property name="browser">*firefox /opt/firefox-9.0.1/firefox-bin</property>
    </extension>
    
    <extension qualifier="ajocado-firefox10">
        <property name="browser">*firefox /opt/firefox-10.0.1/firefox-bin</property>
    </extension>

    <extension qualifier="webdriver">
        <property name="chromeBinary">/opt/google/chrome/chrome</property>
        <property name="chromeSwitches">--user-data-dir=/tmp --disable-web-security</property>
    </extension>

</arquillian>
